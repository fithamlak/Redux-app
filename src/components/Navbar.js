 import { CartIcon} from '../icons';
 import { useSelector } from 'react-redux';

 const Navbar = () => {
    return (
        <nav>
            <div nav-center>
                <h3>Redux toolkit</h3>
            </div>
        </nav>
    )
 }

 export default Navbar;